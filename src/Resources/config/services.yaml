services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false
        bind:
            $exportPaths: '%wexample_symfony_pseudocode.paths%'

    Wexample\SymfonyPseudocode\:
        resource: '../../{Command,Service}'
        tags: ['controller.service_arguments']
