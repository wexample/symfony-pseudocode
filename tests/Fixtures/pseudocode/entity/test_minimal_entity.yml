items:
  - type: class
    name: TestMinimalEntity
    description: 'A Symfony entity that only contains an id in a flat notation'
    properties:
      - name: id
        type: int
        default: null
        description: 'The identifier of the entity.'
        # TODO extract data from Doctrine.
    #        database:
    #          nullable: false
    #          primary: true
    #          auto_increment: true
    methods:
      - name: getId
        description: 'Returns the unique id of the entity instance.'
        type: method
        return: int
